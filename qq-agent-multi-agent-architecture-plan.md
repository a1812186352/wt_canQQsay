# QQ Agent 多Agent系统架构方案

> 目标：实现小Q智能管家、画像记忆、按联系人授权、上下文长度自动总结
> 日期：2026-05-02
> 版本：v2.0

---

## 一、系统能力

| 组件 | 核心能力 | 触发方式 | 输出 |
|------|---------|---------|------|
| **小Q 智能管家** | 消息缓冲、阈值总结、联系人记忆 | 消息收发事件驱动 + 手动触发 | 内容总结、记忆面板 |
| **聊天 Agent** | 回复建议、话术优化、精华提炼 | 用户主动 (悬浮球) | 即时建议 |
| **画像系统** | 联系人画像存储、自然语言展示 | 事件驱动自动更新 | 画像数据注入 prompt |

---

## 二、核心架构

```
┌─────────────────────────────────────────────────────────────┐
│                      前端 UI 层 (index.html)                 │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │ 聊天窗口 │  │  悬浮球  │  │ 推送Toast│  │ 小Q面板  │   │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘   │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    Agent 编排层 (JS Classes)                  │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐            │
│  │ XiaoQMemory│  │ ProfileMgr │  │BehaviorLog │            │
│  │ (消息缓冲) │  │ (画像CRUD) │  │ (事件埋点) │            │
│  │ 阈值总结   │  └────────────┘  └────────────┘            │
│  │ 联系人记忆 │  ┌────────────┐  ┌────────────┐            │
│  │ 按人授权   │  │ FriendAgent│  │ProfileUpdt │            │
│  └────────────┘  │ (推送决策) │  │ (画像更新) │            │
│                  └────────────┘  └────────────┘            │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                   存储层 (localStorage)                       │
│  profile_{id} │ behavior_logs │ xiaoq_memory │ push_queue   │
│  xiaoq_buf_{id} │ xiaoq_contact_perms │ chat_msgs_{id}     │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    LLM 服务层                                 │
│  DeepSeek / 通义千问 / Ollama (OpenAI 兼容接口)              │
└─────────────────────────────────────────────────────────────┘
```

**关键设计决策**：
- 纯浏览器端运行，零后端依赖，存储用 localStorage
- 不同用户访问 GitHub Pages 时数据完全隔离
- 未配置 API 时使用内置 Mock 数据，全部功能可体验
- 画像数据注入到聊天 Agent 的 prompt 中，让回复更个性化

---

## 三、小Q 智能管家设计

### 3.1 核心机制

| 能力 | 描述 | 实现 |
|------|------|------|
| **按联系人授权** | 每个好友/群聊独立设置监测权限 | 设置面板 → 选中联系人 → 开关 |
| **消息缓冲** | 收发消息实时写入联系人专属缓冲区 | `xiaoq_buf_{contactId}` |
| **上下文长度** | 缓冲区消息数达到用户预设阈值自动触发总结 | 默认 15 条，可调 5-100 |
| **内容总结** | 基于缓冲区消息生成自然语言对话总结 | 规则引擎（Mock）/ LLM 生成 |
| **联系人记忆** | 总结内容持久化存储，面板中展示 | `contact_memories` 结构 |

### 3.2 权限模型

每个联系人独立的三项设置：

| 权限项 | 说明 | 默认值 |
|--------|------|--------|
| 记录聊天消息 | 是否将该联系人的对话写入小Q缓冲 | 开启 |
| 自动内容总结 | 缓冲区达到阈值后是否自动生成总结 | 开启 |
| 上下文长度 | 触发自动总结的消息数量阈值 | 15 条 |

开启"自动总结"会自动打开"记录消息"；关闭"记录消息"会自动关闭"自动总结"。

### 3.3 自动总结流程

```
消息发送/接收
      │
      ▼
bufferMessage(contactId, msg)    ← 检查权限
      │
      ▼
缓冲区大小 >= 上下文长度？
      │
   ┌──┴──┐
   │ YES  │──→ _autoSummarize() ──→ 保存到 contact_memories
   └─────┘         │
                   ├─ 统计：消息条数、发送方比例
                   ├─ 话题：关键词匹配提取
                   ├─ 末尾上下文：最近 3 条消息摘要
                   └─ 保留最后 5 条作为上下文衔接
```

---

## 四、画像数据结构

**存储 Key**: `profile_{contact_id}`

```json
{
  "contact_id": "xiaoming",
  "contact_name": "小明",
  "contact_type": "friend",
  "basic_info": {
    "relationship": "close_friend",
    "tags": ["同事", "吃货"]
  },
  "communication_style": {
    "tone": "casual",
    "preferred_topics": ["美食", "技术"],
    "avoided_topics": []
  },
  "interaction_history": {
    "total_messages": 0,
    "last_active_date": null
  },
  "agent_insights": {
    "personality_summary": "开朗外向，喜欢美食",
    "communication_tips": "可以用轻松幽默的语气"
  }
}
```

设置面板中展示为自然语言，而非 JSON。

---

## 五、小Q 记忆数据结构

**存储 Key**: `xiaoq_memory`

```json
{
  "version": 2,
  "contact_memories": {
    "xiaoming": {
      "summaries": [
        {
          "content": "最近和小明聊了15条消息（我说6条，对方9条），涉及周末、聚餐。末尾话题：我说明天有空；小明说那周六见。",
          "msg_count": 15,
          "threshold": 15,
          "ts": 1714675200000
        }
      ],
      "total_messages": 23,
      "last_msg_ts": 1714675300000
    }
  },
  "stats": {
    "total_messages": 45,
    "total_summaries": 2
  }
}
```

---

## 六、文件结构

```
docs/
├── index.html              # 页面壳 + 设置面板
├── styles.css              # 样式（含小Q面板、权限开关）
├── js/
│   ├── config.js           # 联系人、Prompt、Mock数据、XiaoQMemory
│   ├── app.js              # 主应用：UI渲染、消息流、权限管理
│   ├── profile-manager.js  # 画像存储与读写
│   ├── behavior-logger.js  # 行为事件日志
│   ├── friend-agent.js     # 定时分析 + 推送
│   └── profile-updater.js  # 画像自动更新
├── profiles/               # 联系人画像（fetch 加载，修改即生效）
│   ├── xiaoq.json
│   ├── xiaoming.json
│   ├── xiaohong.json
│   └── project_group.json
└── config/
    └── agent-rules.json    # 推送规则
```

---

## 七、风险与降级

| 风险 | 缓解 |
|------|------|
| LLM 调用失败 | 降级到规则引擎 + Mock 数据 |
| localStorage 满 | 缓冲区限制条目数，记忆限制 summaries 数量 |
| 旧版数据不兼容 | load() 自动检测版本号并迁移 |
| 图片/文件不加载 | GitHub Pages 仅需 /docs 目录，无需构建 |

---

**文档版本**: v2.0
**日期**: 2026-05-02
